"""
Multimodal measurement service.

Second try-on option: a frontal photo plus a photo of a real frame are sent to a
multimodal model, which returns the proposed optical measurements. Independent of the
3D generation pipeline — it shares nothing with `services/inference/`.
"""
