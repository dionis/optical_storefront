// Configuración de funcionalidades del sitio (feature flags).
// Interruptor global del PROBADOR VIRTUAL (AR / try-on).
// Ponlo en `true` para volver a activarlo en todo el sitio.
export const TRYON_ENABLED = false;
